project: kinguru
configVersion: 1
---
image: kinguru
from: node:18-alpine
git:
  - add: /
    to: /app
shell:
  beforeInstall:
    - apk add --no-cache libc6-compat
  install:
    - cd app
    - npm install
    - npx prisma generate
  setup:
    - cd app
    - npm run build